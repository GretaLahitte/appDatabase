//renvoie un objet associant un type de données a un composant
import {DialogBase} from "./dialog.base";
import {DialogField} from "./dialgo.field";

export const mappings= {
    Base : DialogBase,
    Field: DialogField
};